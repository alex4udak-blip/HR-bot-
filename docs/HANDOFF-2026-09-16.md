# Состояние на 16.09.2026

Полная переписка по этим работам выгружена отдельным файлом
(`session-export-*.zip`, лежит в Downloads у владельца) — здесь только выжимка.

## Что сделано за 10–16 сентября

| Коммит | Что |
|---|---|
| `651fa30` | «Только мои» включена по умолчанию в своих воронках, в чужих — нет |
| `0ea6d55`, `cb36bf9`, `e42fd80` | F5 больше не принимается за переход по ссылке; воронка возвращается в прежнем виде |
| `d15595a` | «Вакансии: Мария» работает у всех ролей; админ не ниже рекрутёра в «Заявках», может «Взять в работу» и «Отказаться» |
| `cb71fd5` | «Выйти из вакансии» и «Закрыть вакансию» — отдельные кнопки с понятными подтверждениями |
| `10e80f9`, `14eb6c5` | Рекрутёр правит любого кандидата (теги у имени, потом все поля) |
| `3e347c6` | Расширение временно ходит на `saturn.ac` (у `enceladus.site` петля редиректов) |
| `99dec6b` | Дубли решаются попарно: кнопки действуют на анкету, открытую справа |
| `794fe57` | Смена этапа уходила заявке предыдущего открытого кандидата — исправлено |
| `f55a650` | В воронке видны комментарии всех, кто в ней работает |
| `f501270` | Удаление записи истории НЕ двигает кандидата (отмена `506ed69`) |
| `e4c10c3` | `expected_entity_id` + логи `STAGE_CHANGE` / `STAGE_GUARD` / `HISTORY_DELETE` |

## Открытые вопросы

1. **Домен `enceladus.site`** — A-записи смотрят на 157.180.57.47, нужен
   135.181.120.90 (saturn2). Меняет владелец у регистратора. После починки
   вернуть расширение на `enceladus.site` (см. пометки «ВРЕМЕННО» в
   `backend/chrome-extension/background.js` и `popup.js`).
2. **Переменные бэкенда в Saturn**: `MINIAPP_URL` указывает на `enceladus.site`
   (мини-апп в Telegram не открывается), `FRONTEND_URL` — внутренний адрес
   контейнера (ломает кнопки «Открыть» в уведомлениях). Правит владелец.
3. **Деплой фронта**: бывает, что Saturn пишет «успешно», а сайт отдаёт старую
   сборку. Проверять по содержимому чанка (рецепт в CLAUDE.md).
4. **Дубли** — согласован план (этап 1 без миграций): единый матчер, отчества,
   телефоны E.164, убрать текст резюме из слияния, перепроверка после правки
   карточки, предупреждение до создания (не запрет), удалить мёртвый код
   (`services/duplicates.py`, `DuplicateWarning.tsx`, `POST /entities/{id}/merge`).
   Этап 2 — таблица решений по парам, этап 3 — таблица идентификаторов и очередь
   «Дубли на проверку».
5. **Не проверено**: не искали других кандидатов, пострадавших от бага со сменой
   этапа (когда этап уходил соседнему). Владелец решил отложить.
6. **После деплоя** `794fe57`/`f501270`: Марии перевести Глушко Дениса в «Отказ»
   вручную и подтвердить, что Мец Захар остаётся в отказе.
